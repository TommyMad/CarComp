package pl.CarComp.database.dao;

import com.j256.ormlite.support.ConnectionSource;

public class CarVersionDao extends CommonDao{
    public CarVersionDao(ConnectionSource connectionSource) {
        super(connectionSource);
    }
}
